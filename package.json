{
  "name": "storage-advanced",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy:nft": "npx hardhat run scripts/deploy-nft.js --network sepolia",
    "mint:nft": "npx hardhat run scripts/mint-nft.js --network sepolia",
    "upload:ipfs": "node scripts/uploadToIPFS.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.20.2"
  },
  "dependencies": {
    "@pinata/sdk": "^2.1.0",
    "axios": "^1.11.0",
    "pinata": "^2.4.9"
  }
}
